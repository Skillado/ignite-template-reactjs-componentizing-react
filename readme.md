# This project was made to acquire knowledge in react and concepts of components using createContext, useContext and useReducer

### For launch project run:

>for run aplication

	`yarn dev 	`

>for run JSON Server

	`yarn server	`
